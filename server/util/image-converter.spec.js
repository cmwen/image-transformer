describe("Image converter", () => {
    xit("should abstract external image converter", () => {});
    xit("should support jpeg, webp, png image format", () => {});
    xit("should have a method to get width and height of an image", () => {});
    xit("should able to convert and resize to a different format", () => {});
});
