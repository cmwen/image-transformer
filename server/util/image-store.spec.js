describe("Image store provides a abstract layer for accessing real storage. Actual storage can be anything.", () => {
    xit("should provide a method to store image", () => {});
    xit("should provide a method to get image", () => {});
});
