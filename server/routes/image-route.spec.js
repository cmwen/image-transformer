describe("Image service express router", function() {
    it("should handle POST request and delicate request to image controller when request parameters is valid", () => {});

    it("should return 400 when user provide incorrect parameters", () => {});

    it("should return 500 when an internal error when during image conversion", () => {});
});
