describe("Image service routes", function() {
    xit("should handle POST request and delicate request to image controller when request parameters is valid", () => {});

    xit("should return 400 when user provide incorrect parameters", () => {});

    xit("should return 500 when an internal error when during image conversion", () => {});

    xit("should handle GET request and delicate request to image controller", () => {});
});
